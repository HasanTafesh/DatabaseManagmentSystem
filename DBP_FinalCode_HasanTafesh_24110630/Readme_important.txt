Run the project using this command:

- gcc -I include src/*.c -o (universiy_dbms_final) <-- the output executable name

- you can directly run the project using the executable file in the folder (university_dbms_final.exe)